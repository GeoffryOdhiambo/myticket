; Event images are small; ticket PDFs (dompdf) need a little headroom.
memory_limit=256M
upload_max_filesize=16M
post_max_size=18M
max_execution_time=60
expose_php=Off

log_errors=On
error_log=/dev/stderr
display_errors=Off
